{
  "name": "typedoc-plugin-coverage",
  "version": "2.0.1",
  "main": "index.js",
  "license": "MIT",
  "author": "Gerrit Birkeland <gerrit@gerritbirkeland.com>",
  "engines": {
    "node": ">= 14"
  },
  "devDependencies": {
    "@types/node": "^17.0.23",
    "prettier": "^2.8.7",
    "typedoc": "^0.24.2",
    "typescript": "^5.0.4",
    "vitest": "^0.30.1",
    "esbuild": "^0.17.16"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Gerrit0/typedoc-plugin-coverage.git"
  },
  "files": [
    "index.js"
  ],
  "keywords": [
    "typedocplugin"
  ],
  "peerDependencies": {
    "typedoc": "^0.22.12 || 0.23.x || 0.24.x"
  },
  "scripts": {
    "pretest": "npm run build",
    "test": "vitest run",
    "typecheck": "tsc",
    "build": "esbuild index.ts --outfile=index.js --bundle --target=node14 --platform=node --external:typedoc"
  }
}
