{
  "name": "typedoc-plugin-coverage",
  "version": "1.0.1",
  "main": "index.js",
  "license": "MIT",
  "author": "Gerrit Birkeland <gerrit@gerritbirkeland.com>",
  "devDependencies": {
    "@types/node": "^17.0.23",
    "prettier": "^2.6.1",
    "typedoc": "^0.22.13",
    "typescript": "^4.6.3",
    "vitest": "^0.7.11",
    "esbuild": "^0.14.28"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Gerrit0/typedoc-plugin-coverage.git"
  },
  "files": [
    "index.js"
  ],
  "keywords": [
    "typedocplugin"
  ],
  "peerDependencies": {
    "typedoc": "^0.22.12"
  },
  "scripts": {
    "pretest": "npm run build",
    "test": "vitest run",
    "typecheck": "tsc",
    "build": "esbuild index.ts --outfile=index.js --bundle --target=node12 --platform=node --external:typedoc"
  }
}
